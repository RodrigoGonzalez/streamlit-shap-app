site_name: SHAP App

docs_dir: docs

theme:
  name: "material"
  features:
    - navigation.tabs
    - navigation.indexes
    - navigation.instant

plugins:
  - search:
  - mkdocstrings:
      handlers:
        python:
          paths: [ src ]
  - render_swagger:
      title: API Reference

nav: [ ]

repo_url: https://github.com/RodrigoGonzalez/streamlit-shap-app

# to navigate with `/site` content correctly
use_directory_urls: false
